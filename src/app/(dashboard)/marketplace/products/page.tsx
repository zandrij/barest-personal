export async function getServerSideProps() {
    return {
        props: {
            
        }
    }
}

const Products = () => {
    return (
        <>Products</>
    );
};

export default Products;