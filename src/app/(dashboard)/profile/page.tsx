export async function getServerSideProps() {
    return {
        props: {
            
        }
    }
}

const Profile = () => {
    return (
        <>Profile</>
    );
};

export default Profile;

